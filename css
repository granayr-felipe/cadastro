/* Estilos para o cabeçalho */
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 30px;
    background-color: #00dae0; /* Cor de fundo do cabeçalho */
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); /* Sombras para dar profundidade */
  }
  
  .header_logo .logo {
    max-height: 50px; /* Ajusta a altura da logo */
    max-width: 100%;
  }
  
  .nav_menu ul {
    list-style-type: none;
    margin: 0;
    padding: 0;
    display: flex;
  }
  
  .nav_menu li {
    margin-right: 20px;
  }
  
  .nav_menu li:last-child {
    margin-right: 0;
  }
  
  .nav_menu a {
    color: #fff;
    text-decoration: none;
    font-size: 18px;
    font-family: 'Open Sans', sans-serif;
    transition: color 0.3s ease;
  }
  
  .nav_menu a:hover {
    color: #333; /* Cor ao passar o mouse sobre o link */
  }
  
  /* Estilo para dispositivos móveis */
  @media (max-width: 768px) {
    .header {
      flex-direction: column;
      align-items: flex-start;
    }
  
    .nav_menu ul {
      flex-direction: column;
    }
  
    .nav_menu li {
      margin-bottom: 10px;
    }
  }
  